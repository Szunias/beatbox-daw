# Production Environment Configuration for GitHub Pages
# VITE_WS_URL is left empty to skip WebSocket connection and trigger demo mode immediately
# This is intentional for GitHub Pages deployment where no backend is available
# To connect to a deployed backend, set this to the WebSocket URL: wss://your-backend-domain.com:8765
VITE_WS_URL=
